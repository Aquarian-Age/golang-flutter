name: cli
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  http: ^0.13.5
  http_client: ^1.5.1
  my_app: ^1.0.0
  sprintf: "^6.0.0"
  date_format: ^2.0.7

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0

